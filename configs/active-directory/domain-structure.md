# Active Directory Domain Structure

The lab uses the `cbc.lab` domain with separate Organizational Units for different regions.

![Active Directory Domain Structure](AD_TREE_STRUCTURE.PNG)

```text
cbc.lab
├── Africa
│   └── Users
├── Europe
│   └── Users
├── Computers
└── Domain Controllers
